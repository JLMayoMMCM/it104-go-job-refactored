// This is a placeholder for the actual PDF.js worker file.
// In a production environment, you would copy the actual pdf.worker.min.js file
// from the pdfjs-dist package to this location.
// For now, we'll set the worker source to a CDN as a fallback if the file isn't found.

if (typeof window !== 'undefined') {
  // Fallback to CDN if local worker file is not found
  window.PDFJS_WORKER_SRC = window.PDFJS_WORKER_SRC || `//cdnjs.cloudflare.com/ajax/libs/pdf.js/2.11.338/pdf.worker.min.js`;
}

console.log('PDF Worker loaded');
